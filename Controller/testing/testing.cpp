#include "testing.h"

testing::testing(QObject *parent) : QObject(parent)
{

}

//bool push_data_into_queue_to_send(sdo_msg_buff mailbox){
//    sdo_send_mailbox *p_mail = &SDO_mailbox;
//    memcpy(&p_mail->sdo_send_msg[p_mail->msg_waiting],&mailbox,sizeof(sdo_msg_buff));

//}

